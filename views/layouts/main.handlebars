<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    {{#each styles}}
    <link rel="stylesheet" href="{{this}}">
    {{/each}}
</head>

<body>
    {{{body}}}
    {{#each scripts}}
    <script src="{{this}}"></script>
    {{/each}}
</body>

</html>